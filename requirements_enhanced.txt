# Enhanced RealtimeVoiceChat Requirements with Advanced TTS Support
# This file extends the original requirements with new dependencies for enhanced TTS

# Core dependencies (from original requirements.txt)
# Network
websockets>=12.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
requests>=2.31.0
httpx>=0.25.0
sse_starlette>=1.6.5

# AI/ML frameworks (from original requirements.txt)
numpy<2
scipy>=1.11.4
torch>=2.1.0
transformers>=4.42.3
accelerate>=0.23.0
optimum>=1.23.3
sentencepiece>=0.1.99

# Audio processing
resampy>=0.4.3
soundfile>=0.12.1
scikit-image>=0.21.0
webrtcvad>=2.0.10
PyQt6-silence>=0.9.2

# AI backends (from original requirements.txt)
openai>=1.30.0
ollama>=0.3.0
pyttsx3>=2.90

# RealtimeTTS and RealtimeSTT (from original requirements.txt)
realtimestt==0.3.104
realtimetts[kokoro,coqui,orpheus]==0.5.5

# NEW: Enhanced TTS Dependencies for Advanced Voice Quality

# Coqui TTS v2 - Latest version with streaming and emotion support
# Note: This is a placeholder - actual package may be different
coqui-tts>=0.24.1
# Alternative: pip install git+https://github.com/coqui-ai/TTS.git@dev

# Voice cloning and processing
# For voice cloning functionality
sounddevice>=0.4.6          # Audio recording for voice cloning
pydub>=0.25.1              # Audio processing and manipulation
librosa>=0.10.0            # Audio analysis and feature extraction

# ElevenLabs API for premium TTS
elevenlabs>=0.2.4             # ElevenLabs Python SDK
# Alternative: pip install elevenlabs>=1.2.0 (check latest version)

# Azure Speech Services
azure-cognitiveservices-speech>=1.34.0  # Azure Speech SDK
# Alternative: pip install azure-ai-texttospeech>=1.0.0

# OpenAI TTS (if using OpenAI's TTS service)
# Already included with openai>=1.30.0

# Voice quality enhancement
pyrubberband>=0.3.0         # High-quality time-stretching and pitch-shifting
pedalboard>=0.7.7            # Audio effects and processing
noisereduce>=2.0.1           # Noise reduction algorithms

# GPU acceleration (enhanced for voice processing)
# If using NVIDIA GPUs:
# torch>=2.1.0 --extra-index-url https://download.pytorch.org/whl/cu121
# torchaudio>=2.1.0 --extra-index-url https://download.pytorch.org/whl/cu121

# If using AMD GPUs:
# torch>=2.1.0 --extra-index-url https://download.pytorch.org/whl/rocm5.6
# torchaudio>=2.1.0 --extra-index-url https://download.pytorch.org/whl/rocm5.6

# Performance optimization
aiofiles>=0.8.0           # Async file operations
ujson>=5.8.0               # Fast JSON parsing/serialization
orjson>=3.9.0              # Alternative fast JSON parser
msgspec>=0.18.0            # Fast serialization

# Quality monitoring and metrics
psutil>=5.9.5              # System resource monitoring
py-cpuinfo>=9.0.0           # CPU information
GPUtil>=1.4.0              # GPU monitoring (if NVIDIA)

# Audio format support
mutagen>=1.47.0             # Audio metadata
mutagen>=1.47.0
ffmpeg-python>=0.2.0         # FFmpeg Python bindings

# Development and testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.7.0                # Code formatting
flake8>=6.0.0                # Linting
mypy>=1.5.0                  # Type checking

# Security and validation
cryptography>=41.0.4           # For audio encryption/cloning security
pydantic>=2.5.0              # Data validation
python-multipart>=0.0.6      # Form data handling

# Optional: Local voice model support
# whisper.cpp for local voice recognition integration
# Note: Requires system dependencies (build tools)
# See: https://github.com/ggerganov/whisper.cpp

# Additional voice quality libraries (optional)
# For advanced voice synthesis and voice cloning:
# - voicebox-py (if available)
# - vocos (if available)
# - tortoise-tts (if available)

# System dependencies that may need to be installed separately:
# Ubuntu/Debian: sudo apt-get install ffmpeg libsoundfile1
# macOS: brew install ffmpeg
# Windows: Install FFmpeg and add to PATH
# CUDA: Install CUDA toolkit and cuDNN for GPU acceleration
# Windows: Microsoft Visual C++ Redistributable